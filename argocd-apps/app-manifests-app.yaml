apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: k8s-app-manifests
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/your-org/your-repo.git' # Thay bằng link Git chứa code
    targetRevision: main
    path: app-manifests # Trỏ vào thư mục chứa cấu hình ứng dụng (test-app, bad-pod...)
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: test-namespace # Sẽ deploy các pod vào namespace này
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true # Tự động tạo Namespace nếu chưa có
